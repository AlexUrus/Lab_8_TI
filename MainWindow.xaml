<Window x:Class="Lab_3_TI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lab_3_TI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <TextBox x:Name="Message" HorizontalAlignment="Left" Margin="36,19,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="142" TextChanged="Message_TextChanged"/>
        <DataGrid x:Name="InputDataGrid" ItemsSource="{Binding}" Height="NaN" Margin="0,58,437,0" Width="NaN">
            <DataGrid.Columns>
                
            </DataGrid.Columns>
        </DataGrid>
        <TextBox x:Name="EncodeMessage" HorizontalAlignment="Left" Margin="529,19,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120"/>
        <DataGrid x:Name="OutputDataGrid" ItemsSource="{Binding}" Height="NaN" Margin="389,58,11,0" Width="NaN">
            <DataGrid.Columns>

            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
